[project]
name = "niyan"
version = "0.1.0"
description = "Niyān CLI and Python filesystem client"
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [
    { name = "Niyān contributors" },
]
keywords = ["data-version-control", "fsspec", "git-lfs", "datasets"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: Education",
    "Intended Audience :: Science/Research",
    "Operating System :: POSIX",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Typing :: Typed",
]
dependencies = [
    "fsspec>=2025.3.0",
    "keyring>=25.6.0",
]

[project.urls]
Homepage = "https://github.com/aryan-f/Niyan"
Documentation = "https://github.com/aryan-f/Niyan#readme"
Issues = "https://github.com/aryan-f/Niyan/issues"
Source = "https://github.com/aryan-f/Niyan"

[project.scripts]
niyan = "niyan.cli:main"

[project.entry-points."fsspec.specs"]
niyan = "niyan.filesystem:NiyanFileSystem"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/niyan"]

[tool.hatch.build.targets.sdist]
include = [
    "/LICENSE",
    "/README.md",
    "/pyproject.toml",
    "/src",
    "/tests",
    "/uv.lock",
]

[dependency-groups]
dev = [
    "polars>=1.0",
]
