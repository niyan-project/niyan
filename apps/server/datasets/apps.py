from django.apps import AppConfig


class DatasetsConfig(AppConfig):
    """Configure the Niyān datasets application."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'datasets'
