# Development-only Django proxy target
NIYAN_DJANGO_URL=http://127.0.0.1:8000

# Canonical browser-facing installation origin embedded during the SPA build.
# Use an origin only; URL path prefixes such as /niyan are not supported in v1.
# NIYAN_PUBLIC_URL=https://niyan.example.org
